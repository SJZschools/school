<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tedu.mapper.UserInfoMapper">
	<!--修改用户详细信息 -->
	<update id="updateself">
		update user_info
		<set>
			
			name=#{name}
			sex=#{sex}
			
			
			e_mail=#{eMail}
			address=#{address}
			timeofEnrollment=#{timeofenrollment}
			birthday=#{birthday}
			classes=#{classes}
			tel=#{tel}
			age=#{age}
			sf=#{sf}
			head_address=#{headAddress}
			personal_remark=#{personalRemarks}
			qq=#{qq}
			blood=#{blood}
			job=#{job}
			homeplace=#{homeplace}
		</set>
		where id=#{userInfo.id}
	</update>
</mapper>